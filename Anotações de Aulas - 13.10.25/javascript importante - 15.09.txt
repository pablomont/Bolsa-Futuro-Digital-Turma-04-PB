---------------baixar pacote pra o código javascript funcionar:

npm install prompt-sync

-----------coverter horas em minutos:

function convertHoursToMinutes(hours) {
    min = hours * 60;
    return min;
}

----------------ou 
function convertHoursToMinutes(hours) {
    var min = hours * 60;
    return min;
}
const result = convertHoursToMinutes(2);
console.log(result);

