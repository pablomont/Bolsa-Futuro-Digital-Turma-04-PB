export class Pizza {
  constructor(public ingredientes: string[]) {}
  const = new Pizza(["calabresa", "queijo",]);
  calculaPreco(){
    this.ingredientes.length <=2 
    this.ingredientes.length <=5
  }
}

